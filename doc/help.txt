1、在U盘根目录下创建文件夹：「tvbooks」，并在其根目录下创建名为「menu.txt」的json文件，如需支持中文请用utf-8格式。支持二级菜单，在U盘体现就是二级目录。

2、图声并茂的绘本模式，type请填：audio_image，如果仅仅是音频播放，type请填：audio

3、如果绘本的根目录下有索引文件「index.txt」(一行一个子文件夹名)则按照索引文件的顺序播放，不带的话App会自动遍历子目录，作为播放列表。

3、音频目录不用设置索引文件，App自动遍历文件，并按照顺序播放。
